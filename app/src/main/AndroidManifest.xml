<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.sbsistemas.minhacarteira">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListaGrupos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FormularioCategoriaActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".FormularioContaActivity" />
        <activity android:name=".ListaCategoriasActivity" />
        <activity android:name=".ListaContasActivity" />
        <activity android:name=".ListaReceitasActivit" />
        <activity android:name=".FormularioReceiraActivity" />
        <activity android:name=".EditaContaActivity" android:windowSoftInputMode="adjustPan" />
    </application>

</manifest>